import { motion } from 'motion/react';
const About = () => {
  return (
    <motion.div className="h-[100vh]">
      <h1 className="text-section-title">ABOUT</h1>
    </motion.div>
  );
};

export default About;
